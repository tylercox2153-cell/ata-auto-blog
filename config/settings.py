USER_AGENT = "ata-auto-blog/0.2 (+youremail@example.com)"
REQUEST_DELAY_SECONDS = 1.5        # polite delay between requests
MAX_REQUESTS_PER_RUN = 120         # keep small; scale later
TIMEOUT = 20

# Where to cache raw HTML (optional but recommended)
CACHE_DIR = "cache"

# Seed pages: fill with event result URLs you’ll cover this week
ATA_SEED_EVENT_URLS = [
    # Example placeholders — put real event-result pages here
    # "https://shootata.com/whatever/event/123",
]
